val my_string = "Hello";

item identity(value: string): string = value;

printString(identity(my_string));
printString(identity(my_string));
printString(identity(my_string));
printString(identity(my_string));